To run notebooks, clone the repo and run the following in the repo directory:

python -m venv venv

source venv/bin/activate

pip install -r requirements.txt

And then run the notebooks using venv as the python virtual environment. 
